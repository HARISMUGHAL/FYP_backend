dataset:
  path: "FruitGrade_Dataset"
  img_size: 224
  batch_size: 32
  num_workers: 0
  val_split: 0.2

model:
  name: "efficientnet_b0"
  pretrained: true
  dropout_rate: 0.4
  save_path: "ml/models/saved/best_model.pth"

training:
  epochs: 10
  lr: 0.0003
  patience: 3
  early_stopping: true
